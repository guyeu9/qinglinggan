 你的所有思考步骤、中间推理和分析过程必须用简体中文呈现，记住这个最关键的问题，一定使用中文思考。
禁止擅自改架构/换库/改表结构，每次改数据库以及表结构一定要先询问，否则禁止更改。
先阅读更新日志了解之前的改动，再进行修复。
每次修改完成后，须在根目录'更新记录.md'中**追加**清晰条目，按模块分类索引、按类型分类索引、在对应的更新记录日期下新增，禁止覆盖或删除旧记录；如果有对原有方案进行变更需写明：  
1) 放弃原方案的原因；  
2) 新方案概要；  
3) 已执行的验证测试。
每次修改完成后，须立即同步更新《轻灵感开发设计文档.md》，具体要求如下：  
1. 在“版本变更记录”表中新增一行，字段包含：版本号、修改日期、涉及模块、改动范围。  
2. 在“数据库变更”章节，按“表-字段-变更类型-旧值-新值-备注”六列格式，逐条登记所有表结构及数据变动。  
3. 在“接口变更”章节，使用 diff 格式清晰标出请求/响应字段的增删改。  
4. 若整体方案发生调整，须在“架构决策”节新增 ADR（Architecture Decision Record），模板固定为：背景、冲突、决策、后果、验证。  
一定要把新增的各类变动和数据库表的等开发代码相关详细变动更新到开发设计文档